flag 변수
	어디까지 들어왔는지 확인하기 위한 변수
	변수의 값을 바꿔줌으로써
	해당 코드가 어느 영역에 도달했는지 판단할 수 있다
	보통 boolean타입을 사용한다

Set
	순서가 없이 저장된다
	중복을 허용하지 않는다
HashSet
	HashSet<클래스타입> 세트명 = new HashSet<클래스타입>();

Map
	key value가 한쌍으로 저장되는 구조

	순서가 없다
	key는 중복 x, value는 중복 o

HashMap
HashMap<key, value> 맵 이름 = new HashMap<>();